<div id="user_state_code_wrapper">
  <% parent_region ||= params[:parent_region] %>
  <% country = Carmen::Country.coded(parent_region) %>

  <% if country.nil? %>
      <%= text_field_tag :state, 'Please Select Any Country', disabled: true %>
  <% elsif country.subregions? %>
      <%= subregion_select(:user, :state, parent_region, {}, { :class => 'profile-dropdown-color' }) %>
  <% else %>
      <%= text_field(:user, :state) %>
  <% end %>
</div>