$('.btn.has-spinner').removeClass('active');
<% if @login_success %>
    window.location.href = "<%= current_user.profile_complete? ? explores_path : profile_profile_index_path %>";
<% else %>
console.log("-------------------------------<%= @messages %>");
$('#sign-in-validation-error').html("<%= j render(:partial => 'shared/errors', :locals => { messages: @messages }) %>" );
<% end %>