- if @service.is_error
	$("#btnGo").removeAttr('disabled')
- else
	$("#results").append("#{escape_javascript(render partial: "list_posts", locals: {resp: @resp})}");
	$("#btnGo").removeAttr('disabled')
