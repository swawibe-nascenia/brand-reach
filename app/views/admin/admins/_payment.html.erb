
<tr id="payment-<%= payment.id %>">
  <% bank_account = BankAccount.find(payment.bank_account_id) %>
  <td><%= payment.user.full_name %></td>
  <td><%= bank_account.bank_name %></td>
  <td><%= bank_account.account_name %></td>
  <td><%= bank_account.account_number %></td>
  <td><%= payment.user.balance %></td>
  <td><%= payment.amount_billed %></td>
  <td>
      <% if payment.pending? %>

            <%= link_to 'Payment Complete', 'javascript:;',
                        data: {id: payment.id},
                        class: 'js-make-payment-paid' %>


      <% elsif payment.paid? %>
            <p class="text-success">Paid</p>
      <% else %>
          <p class="text-danger">Fail</p>
      <% end %>
  </td>
</tr>